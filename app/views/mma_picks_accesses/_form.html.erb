<%= form_for([@mma_picks_pool, @mma_picks_access]) do |f| %>
  <% if @mma_picks_access.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mma_picks_access.errors.count, "error") %> prohibited this mma_picks_access from being saved:</h2>

      <ul>
      <% @mma_picks_access.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
